apiVersion: v1
kind: Service
metadata:
  name: {{ include "release-api.fullname" . }}
  labels:
    {{- include "release-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "release-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPortName }}
